function findLongestWord(str) {
  var arr = str.split(' '),
      length = 0;
  for(var i=0;i<arr.length;i++) {
    var tlength = arr[i].length;
    if(tlength > length) {
      length = tlength;
    }
  }
  return length;
}

findLongestWord("The quick brown fox jumped over the lazy dog");
